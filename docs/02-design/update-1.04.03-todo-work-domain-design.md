# Update 1.04.03 Todo Work 영역 설계

- 고정 영역 배열 마지막에 `work`를 추가한다.
- 신규등록 영역 버튼은 모바일 2열, 넓은 화면 4열로 표시한다.
- Work 카드는 기존 slate 색상 체계를 사용한다.
- `life_todos_domain_check`를 갱신해 서버가 `work` 값을 허용하도록 한다.
- 캐시를 `v178`로 갱신한다.
