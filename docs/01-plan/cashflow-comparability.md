# Cashflow comparability — Plan

User correction: changed close status is not a valid exclusion for historical comparison. Separate income and spending; stable Y scale across selected months; actual date X labels.

Plan gate: preserve original cashflow behavior while giving Lab an explicit comparison policy: include latest effective transactions regardless of close status, up to 12 prior ended periods, minimum one eligible period (show n, not unwarranted certainty). Same-elapsed-day comparison still excludes periods shorter than observed day; surface candidate/short-period counts. Annual whole-period spending independently shows every month, with a daily-average toggle to compare unequal lengths. Do not reclose or rewrite financial records.
