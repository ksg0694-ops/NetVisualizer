# Update 1.04.01 patch v02 검증

## 자동검사

- `npm.cmd run check`: 통과
- JavaScript 구문 20개 파일: 통과
- TypeScript 검사: 통과
- 재무 저장소·도메인·월 마감·예측 회귀검사: 통과
- UI 계약·Supabase 계약·정적 자산 검사: 통과

## 화면검증

- 데스크톱 1365×768: 생활 직접 이동, 진행 중 표기, 목록·상세 2열 유지
- 모바일 390×844: 탭 메뉴, 새 할 일 전체화면 폼, 상세 전체화면 폼
- 로그인: 빈 이메일 제출 시 폼 내부 오류 표시
- 모바일·데스크톱 가로 넘침 없음

## 동기화 확인

- `life_todos`에는 인증 사용자별 `delete` RLS 정책이 이미 존재한다.
- 인증된 조회 결과를 로컬 캐시보다 우선하도록 변경했다.
- 수동 데이터 갱신 시 할 일 서버 목록도 다시 읽는다.
- 서버 삭제 실패 시 낙관적으로 제거한 로컬 항목을 복원한다.
- 실제 사용자 할 일을 삭제하는 파괴적 실서버 테스트는 수행하지 않았다.
