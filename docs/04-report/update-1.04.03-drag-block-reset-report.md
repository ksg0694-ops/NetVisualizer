# Update 1.04.03 Drag Ordering / Block Reset Report

## Delivered

- Manual ordering for Learning Archive fields, items, Chapters, and notes.
- Same-parent drop protection and persistent visual order.
- Keyboard reorder fallback on every drag handle.
- Supabase-backed order synchronization across devices.
- `일반 문단` reset option in the Todo block menu.
- Same-block selection now toggles the block back to a paragraph.
- Cache revision advanced to `v162`.

## Result

The two requested interaction gaps are closed without replacing the existing note content format. Existing users can continue editing immediately, while the new ordering metadata synchronizes through the server.
